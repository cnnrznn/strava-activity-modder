/*
 * A proxy for forwarding Strava redirects to 'localhost' to the local minikube cluster
 */

package main

func main() {
	// Call `minikube service activitymodder-backend --url`
	// Open http server on ":8080"
	// redirect request to minikube
}
